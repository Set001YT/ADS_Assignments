package ADS_Assignment_2;

public interface MyList<T> {
    void add(T element);
    T get(int index);
    T remove(int index);
    void set(int index, T element);
    int size();
    boolean isEmpty();
}
